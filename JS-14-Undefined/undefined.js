// Undefined -> sebuah tipe data
// Sebuah variable yang belum ditambahkan nilai, maka variabel tersebut merupakan tipe undefined
// Berbeda dengan null

let name;

if (name == undefined) {
  alert("Data Undefined !!");
} else {
  alert("Data Defined !!");
}
